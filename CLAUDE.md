# Scope

Do not read, write, search, or otherwise access files outside this project
directory (`/Users/wizo/Yammay_Blacksmith/artisan-iron-web`) unless the user
explicitly provides a path outside it or attaches/opens a file from elsewhere
in the current turn. When in doubt, ask before looking outside the project
folder.
